---
topic: 'Async JS'
section: 'JavaScript'
subSection: 'Intermediate'
---

<LearnSectionHeader topic={topic} />

<LearnSectionContent>
  Modern way of fetching data asynchronously:
</LearnSectionContent>

```javascript
const getData = async (url) => {
  const response = await fetch(url)
  const data = await response.json()
  console.log(data)
}
```
